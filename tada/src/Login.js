import React from 'react';
import { Button, Form, Input } from 'antd';

function Login(){
    return(
        <div>
            <Form>
                <Input />
                <Button />
            </Form>
        </div>
    )
}

export default Login;